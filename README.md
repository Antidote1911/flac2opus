# flac2opus
Rust program to convert all .flac files (recursively) in the curent folder and subfolders to .opus.
It use opusenc cli tool (opus-tools must be intalled)


**Usage**
```shell
./flac2opus
```
